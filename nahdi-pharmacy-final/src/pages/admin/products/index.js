// Re-export RetailProducts with a named export
export { default as RetailProducts } from './RetailProducts';

// Also export as default for backward compatibility
export { default } from './RetailProducts';
