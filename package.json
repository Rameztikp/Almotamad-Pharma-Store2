{
  "name": "almutamed-pharmacy",
  "version": "1.0.0",
  "description": "Almotamad Pharmacy Store",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "install-deps": "cd nahdi-pharmacy-final && npm install",
    "build": "cd nahdi-pharmacy-final && npm run build",
    "dev": "cd nahdi-pharmacy-final && npm run dev",
    "deploy": "npm run install-deps && npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
